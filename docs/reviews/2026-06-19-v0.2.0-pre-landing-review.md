# Base Portal v0.2.0 Pre-Landing Review

日期：2026-06-19

## Scope Check

状态：CLEAN

Intent：v0.2.0 做真实 IAM 验收 + 一个第三方应用接入包，不新增配置 UI。

Delivered：新增 no-secret 接入包、ops import dry-run/apply、IAM permission point/group/binding sync dry-run/apply、权限过滤测试、release/deploy 文档和本地验证证据。

## Plan Completion Audit

Plan：`docs/superpowers/plans/2026-06-19-v0.2.0-iam-app-package.md`

- DONE：版本号、部署默认值、`config/portal-apps/base-portal-demo.json`。
- DONE：`OpsService` 接入包校验、导入 dry-run/apply、审计。
- DONE：IAM sync 覆盖 permission points、permission groups、bindings、409 并发补偿和脱敏错误码。
- DONE：auth permission normalization 单测、navigation 无权限/部分权限/完整权限单测。
- DONE：README、DEPLOY、release notes、my-harness 快速索引。
- DONE：本地验证、浏览器截图、console error、审计读回。
- UNVERIFIABLE：真实 Feishu IAM 用户路径和生产 `/version` 读回需要部署后在生产环境验证。

## Review Findings

Pre-Landing Review：2 issues auto-fixed, 0 remaining.

**AUTO-FIXED:**

- `apps/api/src/modules/ops.controller.ts`：`sync-iam-resources` 在空 body 下读取 `body.dryRun`，可能导致 500。修复为先收敛 `Record<string, unknown>`，只接受真实 boolean。
- `apps/api/src/modules/ops.service.ts`：IAM create 在并发 409 且响应不带 id 时可能影响后续 group binding。修复为 409 后重新 list 并用 key 找回 point/group id。

**NEEDS INPUT:**

- None.

## Verification After Fix

```bash
pnpm --filter @base-portal/api test -- ops.service.spec.ts auth.service.spec.ts navigation.service.spec.ts
pnpm --filter @base-portal/api typecheck
pnpm check
pnpm build
BASE_PORTAL_IMAGE=base-portal-release:v0.2.0 docker compose --env-file deploy/.env.example -f deploy/docker-compose.yml config
bash -n install.sh
bash -n upgrade.sh
git diff --check
```

结果：以上命令均返回 exit 0。

## Residual Risk

- 真实 IAM apply 和生产用户路径必须在部署后使用生产环境 secret 与受控 admin session 验证。
- 生产证据必须继续只记录 key、状态、计数、HTTP status 和错误码，不记录 token、OAuth code、cookie、client secret 或 IAM 原始响应体。
