# 2026-06-19 v0.1.1 Ship Preflight

状态：COMPLETE

## 范围

本次收口范围是 Base Portal v0.1.1：

- 生产 pull 策略硬化。
- Dockerfile 基础镜像参数化。
- favicon 静态资源。
- 生产验证证据收口。

## 必须通过

```bash
bash -n install.sh
bash -n upgrade.sh
./install.sh --help >/tmp/base-portal-install-help.txt
./upgrade.sh --help >/tmp/base-portal-upgrade-help.txt
BASE_PORTAL_IMAGE=base-portal-release:v0.1.1 docker compose --env-file deploy/.env.example -f deploy/docker-compose.yml config
pnpm check
pnpm build
git diff --check
git status --short
```

当前已通过：

- `bash -n install.sh`
- `bash -n upgrade.sh`
- `./install.sh --help >/tmp/base-portal-install-help.txt`
- `./upgrade.sh --help >/tmp/base-portal-upgrade-help.txt`
- `BASE_PORTAL_IMAGE=base-portal-release:v0.1.1 docker compose --env-file deploy/.env.example -f deploy/docker-compose.yml config`
- `pnpm check`
- `pnpm build`
- `git diff --check`

最终完成：

- diff review 已完成。
- commit、push、tag、GitHub release 已完成。
- 远端构建并加载 `base-portal-release:v0.1.1`。
- 生产升级和健康验证已完成。

## 生产升级命令

```bash
./upgrade.sh --from v0.1.0 --to v0.1.1 --image base-portal-release:v0.1.1 --pull never
```

如果 release candidate 已经把远端 `.deploy/version` 写为 `v0.1.1`，但 `/version` 或容器环境仍需修正，可以显式使用：

```bash
./upgrade.sh --from v0.1.1 --to v0.1.1 --image base-portal-release:v0.1.1 --pull never --force
```

## 生产验证

- `.deploy/version`：`v0.1.1`
- image id：`sha256:2a34dff5b0ddbd987d4e41cb5922252fae74d89c3d54055dcb1bf90aacb98fd8`
- `/health`：`200 {"status":"ok"}`
- `/ready`：`200 {"status":"ready","checks":{"database":"ok"}}`
- `/version`：`version=0.1.1`，commit 由最终 release tag 和线上读回校验。
- `/favicon.svg`：`200 image/svg+xml`
- Playwright console errors：0
- Playwright screenshot：`output/playwright/base-portal-v0.1.1-production-login.png`

## Secret 边界

不得在聊天、日志、截图、Git 或 release notes 中输出真实 `deploy/.env`、IAM secret、developer API token、数据库密码或证书私钥。
