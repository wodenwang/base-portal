# Base Portal v0.1.0 QA 记录

日期：2026-06-19

## 范围

本记录覆盖 v0.1.0 前端重构切片：

- GitHub issue #1：iframe 标准态移除明显外框和常驻工具栏。
- GitHub issue #2：tab / window 操作迁移到业务 tab 右键菜单。
- GitHub issue #3：菜单默认点击打开内部业务 tab。
- GitHub issue #4：TopBar 用户区收拢为用户下拉菜单。
- GitHub issue #5：窄宽度下 TopBar、菜单、tab、按钮不换行。

不覆盖：

- 生产 Docker Compose 部署尚未执行。
- 生产 smoke 尚未通过。
- release、tag、push 尚未执行。

## 自动化验证

```bash
pnpm --filter @base-portal/portal-web test
pnpm --filter @base-portal/portal-web typecheck
pnpm --filter @base-portal/portal-web lint
pnpm --filter @base-portal/portal-web build
```

结果：全部通过。

后端验证：

```bash
pnpm --filter @base-portal/api test
pnpm --filter @base-portal/api typecheck
pnpm --filter @base-portal/api lint
pnpm --filter @base-portal/api build
```

结果：全部通过。

部署脚本验证：

```bash
bash -n install.sh
bash -n upgrade.sh
docker compose --env-file deploy/.env.example -f deploy/docker-compose.yml config
git diff --check
```

结果：全部通过。

脚本保护分支：

- `./install.sh --skip-dns --skip-nginx` 在未提供生产镜像时拒绝执行。
- `BASE_PORTAL_IMAGE=base-portal:v0.1.0 ./upgrade.sh --from v0.0.9 --to v0.1.0` 拒绝使用本地占位镜像作为生产部署输入。
- `./install.sh --image base-portal-release:v0.1.0 --skip-nginx` 已执行到远端 env 模板创建边界后停止，未启动生产服务，未输出 secret。
- 最新 `./install.sh --image base-portal-release:v0.1.0 --skip-nginx` 会在启动前校验远端 `deploy/.env`，当前只输出问题 key：`FEISHU_IAM_CLIENT_SECRET`、`FEISHU_IAM_DEVELOPER_API_TOKEN`、`POSTGRES_PASSWORD`、`DATABASE_URL`，不输出值。

DNS 和远端预检：

- 阿里云 CLI 已创建并读回 `base-portal.riversoft.com.cn A 120.24.236.92`，记录状态 `ENABLE`。
- 公共解析器 `223.5.5.5` 和 `8.8.8.8` 均读回 `120.24.236.92`。
- 92 服务器 `/home/bpmt/base-portal` 已存在，`deploy/docker-compose.yml`、`deploy/.env.example`、`deploy/.env` 模板和 `releases/v0.1.0` 已创建。
- 生产镜像 `base-portal-release:v0.1.0` 已加载到 92 服务器，远端读回 image id 前缀为 `sha256:33c4ab`。
- 92 服务器 `~/nginx/docker-compose.yml`、`~/nginx/nginx.conf` 和 `/home/bpmt/bpmt-lite/nginx/certs` 存在。
- 现有通配符证书覆盖 `riversoft.com.cn` 和 `*.riversoft.com.cn`，有效期至 2026-08-27。
- `system-nginx` 已配置 `base-portal.riversoft.com.cn` server block，反代到 `host.docker.internal:3000`。
- `https://base-portal.riversoft.com.cn/health` 当前 TLS 校验通过，但返回 `502`，因为应用容器尚未启动。
- 92 服务器 Docker Compose 版本为 `v5.1.3`。

GitHub issues 读回：

- 仓库：`wodenwang/base-portal`
- #1 `调整 iframe 主展示区：移除外框和嵌入工具按钮`：OPEN。
- #2 `将 tab 和窗口操作统一迁移到 tab 右键菜单`：OPEN。
- #3 `统一菜单默认打开方式：菜单点击只打开内部 tab`：OPEN。
- #4 `收拢顶部用户区：点击头像名称后展示退出菜单`：OPEN。
- #5 `窄宽度下导航和菜单文本禁止换行`：OPEN。

## 浏览器验证

运行态：

- 前端 dev server：`http://localhost:5173/`
- API 复用本机已有 `localhost:3000` 容器。
- 未读取或回显 `deploy/.env`。

验证结果：

- mock 登录后进入 Portal shell。
- 点击 `订单中心` 打开内部业务 tab，没有触发默认新窗口。
- 标准 iframe 区域无常驻 toolbar。
- 业务 tab 右键菜单包含 `新窗口打开`、`最大化`、`关闭当前`、`关闭其他`、`关闭全部`。
- 最大化态隐藏 Portal 壳，并保留 `退出最大化`。
- TopBar 用户菜单包含 `退出登录`，TopBar 不再常驻退出按钮。
- 390px 视口下 `scrollWidth=390`，无横向溢出。
- 390px 视口下 tab 高度稳定为 `43px`，TopBar 高度为 `58px`。
- 前端技术栈仍为 React + TypeScript + Vite + CSS variables + lucide-react，未引入 `DESIGN.md` 禁止的大型 UI 框架。
- `rg` 扫描未发现 Ant Design、Material UI、Chakra UI、Arco Design、Element Plus 或 Bootstrap 框架依赖；`bootstrap` 命中仅为本地初始化函数名。

截图证据：

- `output/playwright/base-portal-v0.1.0-desktop-context-menu.png`
- `output/playwright/base-portal-v0.1.0-desktop-user-menu.png`
- `output/playwright/base-portal-v0.1.0-desktop-maximized.png`
- `output/playwright/base-portal-v0.1.0-tablet.png`
- `output/playwright/base-portal-v0.1.0-mobile-drawer.png`
