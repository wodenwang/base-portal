# Base Portal v0.3.0 Functional QA

日期：2026-06-20

## 结论

Step 11 functional QA 通过。v0.3.0 范围内未发现阻塞发布的问题。

## 覆盖项

| 功能 | 结果 | 证据 |
|---|---|---|
| `new_tab` 菜单作为 hint 打开内部业务 Tab | pass | `apps/portal-web/src/workspace.test.ts` |
| 历史 `immersive_iframe` 菜单降级为标准 iframe | pass | `apps/portal-web/src/workspace.test.ts` |
| `refreshTabFrame` 只刷新目标业务 Tab revision | pass | `apps/portal-web/src/workspace.test.ts` |
| Tab 右键菜单刷新当前 iframe | pass | Playwright browser verification |
| Tab 三点菜单刷新当前 iframe | pass | Playwright browser verification |
| 删除沉浸模式用户入口 | pass | Playwright no-immersive checks |
| 最大化仍可用 | pass | Playwright browser verification |
| Ops import 拒绝未知 `openMode` | pass | `apps/api/test/ops.service.spec.ts` |
| Ops import 对历史 `immersive_iframe` 返回 warning 并写入 `iframe` | pass | `apps/api/test/ops.service.spec.ts` |
| SSO Demo 接入包 import apply | pass | 本地 Playwright 通过 `/api/ops/import-app-package` apply |
| Mock 登录后可看到 SSO Demo 菜单 | pass | Playwright browser verification |
| SSO Demo iframe 可加载第三方页面 | pass | Playwright browser verification |

## 命令

```bash
pnpm check
pnpm build
BASE_PORTAL_IMAGE=base-portal-release:v0.3.0 docker compose --env-file deploy/.env.example -f deploy/docker-compose.yml config
bash -n install.sh
bash -n upgrade.sh
git diff --check
```

结果：全部通过。

## 不在本 QA 内完成

- 真实生产 OAuth 用户路径。
- GitHub Release。
- 远端 deploy 和生产 `/version.commit` 对齐。

