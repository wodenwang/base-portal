# Base Portal v0.3.1 Production Deploy Verification

日期：2026-06-27

## 结论

v0.3.1 生产部署通过。部署目标：

- 服务器：`bpmt@120.24.236.92:/home/bpmt/base-portal`
- 域名：`https://base-portal.riversoft.com.cn`
- Docker image：`base-portal-release:v0.3.1`
- 版本文件：`.deploy/version = v0.3.1`

## 构建与上传

```bash
docker buildx build --platform linux/amd64 -f deploy/web.Dockerfile -t base-portal-release:v0.3.1 --load .
```

结果：通过。镜像为 `linux/amd64`。

```bash
docker save base-portal-release:v0.3.1 | ssh bpmt@120.24.236.92 'docker load'
```

结果：通过。远端载入 `base-portal-release:v0.3.1`。

## 升级命令

```bash
GIT_COMMIT=$(git rev-parse HEAD) ./upgrade.sh --from v0.3.0 --to v0.3.1 --image base-portal-release:v0.3.1 --pull never
```

结果：通过。升级脚本完成 `v0.3.0 -> v0.3.1`。

## 生产读回

远端读回：

- `.deploy/version`：`v0.3.1`
- `deploy-web-1`：`base-portal-release:v0.3.1`
- `deploy-db-1`：healthy
- image architecture：`amd64`

公网读回：

- `/health`：`{"status":"ok"}`
- `/ready`：`{"status":"ready","checks":{"database":"ok"}}`
- `/version.version`：`0.3.1`
- `/version.commit`：与部署时 `GIT_COMMIT` 对齐
- `GET /`：`200 text/html`
- `/api/auth/login`：`302` 到 Feishu IAM OAuth authorize

DNS / TLS：

- `base-portal.riversoft.com.cn` 通过 `223.5.5.5` 和 `8.8.8.8` 均解析到 `120.24.236.92`
- 证书 subject：`CN=riversoft.com.cn`
- 证书 issuer：`ZeroSSL ECC DV SSL CA 2`

## Secret 边界

- 未读取、未回显、未提交真实 `deploy/.env`。
- 未输出 IAM client secret、developer API token、OAuth code、access token、cookie、数据库密码或证书私钥。
- 生产证据仅记录版本、状态、计数、HTTP status、公开 DNS/TLS 元数据和脱敏 commit。
