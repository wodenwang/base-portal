# Base Portal v0.3.0 Local Verification

日期：2026-06-20

## 结论

本地工程验证通过。v0.3.0 已完成以下本地门禁：

- 前端 workspace 移除沉浸运行态，新增当前 iframe refresh revision。
- Tab 右键菜单和 Tab 三点菜单均调用同一个刷新当前 iframe 动作。
- 后端接入包 import 对历史 `immersive_iframe` 兼容降级为 `iframe`，并返回非敏感 warning。
- SSO Demo 接入包菜单指向 `https://feishu-iam-sso-demo.riversoft.com.cn/`，不包含 secret、token、cookie 或 authorization code。
- 版本号、部署默认值、release notes 和部署治理已更新到 `v0.3.0`。

## 命令证据

```bash
pnpm --filter @base-portal/portal-web test
```

结果：通过。`2` 个 test files，`16` 个 tests。

```bash
pnpm --filter @base-portal/portal-web typecheck
```

结果：通过。

```bash
pnpm --filter @base-portal/api test
```

结果：通过。`4` 个 test files，`19` 个 tests。

```bash
pnpm --filter @base-portal/api typecheck
```

结果：通过。

```bash
pnpm check
```

结果：通过。覆盖 workspace typecheck、lint、test。

```bash
pnpm build
```

结果：通过。`apps/api` Nest build 通过；`apps/portal-web` Vite production build 通过。

```bash
BASE_PORTAL_IMAGE=base-portal-release:v0.3.0 docker compose --env-file deploy/.env.example -f deploy/docker-compose.yml config
```

结果：通过。Compose 输出使用 `image: base-portal-release:v0.3.0`、`APP_VERSION=0.3.0`、`BASE_PORTAL_VERSION=v0.3.0`。

```bash
bash -n install.sh
bash -n upgrade.sh
git diff --check
```

结果：全部通过。

## 关键覆盖

- `apps/portal-web/src/workspace.test.ts` 覆盖：
  - `new_tab` 作为 hint 打开为内部 iframe。
  - 历史 `immersive_iframe` 打开为内部 iframe，`refreshKey=0`。
  - `refreshTabFrame` 只递增目标业务 Tab 的 revision。
  - 缺失 Tab refresh 为 no-op。
- `apps/api/test/ops.service.spec.ts` 覆盖：
  - 未知 `openMode` 被拒绝为安全 validation error。
  - 历史 `immersive_iframe` dry-run 返回 warning。
  - apply 时持久化 `openMode='iframe'`。
  - IAM sync 错误不泄漏 token 或原始响应体。

## 未完成验证

以下验证待后续 Step 9-15 完成：

- Playwright 桌面、平板、手机浏览器验证。
- 本地 UI 视觉 QA 和功能 QA 文档。
- Git closeout、GitHub Release、远端部署和生产读回。
- 线上 SSO Demo 菜单、fallback 和新窗口恢复路径验证。
