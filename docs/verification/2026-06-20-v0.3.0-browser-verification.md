# Base Portal v0.3.0 Browser Verification

日期：2026-06-20

本地验证地址：`http://127.0.0.1:3103`

## 结论

Step 9 browser verification 通过。使用本地 mock 登录、v0.3.0 Docker Compose 实例和 Playwright 完成桌面、平板、手机验证。

## 环境

- Compose project：`base-portal-v030`
- Image：`base-portal-local:v0.3.0`
- `/health`：`{"status":"ok"}`
- `/ready`：`{"status":"ready","checks":{"database":"ok"}}`
- `/version`：`version=0.3.0`，`commit=local`
- Mock 用户：`mock-user-001`，仅用于本地验证

## Playwright 检查

| Check | Result |
|---|---|
| SSO Demo iframe `src` 为 `https://feishu-iam-sso-demo.riversoft.com.cn/` | pass |
| 页面无用户可见“沉浸模式”文案 | pass |
| 桌面 Tab 右键菜单显示“刷新当前 iframe” | pass |
| 桌面 Tab 右键菜单不显示“沉浸模式” | pass |
| 桌面 Tab 右键刷新后仍只保留当前 SSO Demo iframe | pass |
| Tab 三点菜单显示“刷新当前 iframe” | pass |
| Tab 三点菜单不显示“沉浸模式” | pass |
| 平板 834px Tab 三点菜单显示刷新 | pass |
| 手机 390px Tab 三点菜单显示刷新 | pass |
| 手机 390px Tab 三点菜单显示新窗口打开 | pass |
| 手机 390px Tab 三点菜单不显示“沉浸模式” | pass |
| 最大化仍可进入，并显示“退出最大化” | pass |
| Console errors | `0` |
| Page errors | `0` |

## 截图证据

- `output/playwright/v0.3.0-desktop-sso-demo.png`
- `output/playwright/v0.3.0-desktop-context-refresh.png`
- `output/playwright/v0.3.0-tablet-tab-actions.png`
- `output/playwright/v0.3.0-mobile-tab-actions.png`
- `output/playwright/v0.3.0-desktop-maximized.png`

## 观察

- Base Portal 桌面、平板、手机壳体未发现操作入口重叠或不可用。
- SSO Demo iframe 可加载到第三方页面。该页面显示的是 SSO Demo 自身的公开 OAuth 配置和登录状态，不包含 Base Portal secret、token、cookie 或 authorization code。
- 手机截图中 SSO Demo iframe 内部页面标题较大、内容密度偏高；这是 SSO Demo 自身响应式问题，不阻塞 Base Portal v0.3.0，但应作为外部项目后续优化点。

