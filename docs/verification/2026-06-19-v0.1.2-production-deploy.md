# Base Portal v0.1.2 验证记录

状态：`COMPLETE`

## 本地验证

```bash
pnpm check
pnpm build
BASE_PORTAL_IMAGE=base-portal-release:v0.1.2 docker compose --env-file deploy/.env.example -f deploy/docker-compose.yml config
bash -n install.sh
bash -n upgrade.sh
git diff --check
```

结果：通过。

## 本地运行态验证

- Compose project：`base-portal-v012`
- 镜像：`base-portal-local:v0.1.2`
- URL：`http://127.0.0.1:3102`
- `/health`：`{"status":"ok"}`
- `/ready`：`{"status":"ready","checks":{"database":"ok"}}`
- `/version`：`version=0.1.2`

## Browser QA

- Desktop 1440x900：collapsed flyout sidebar、Tab close fixed right、drag/fallback reorder verified。
- Tablet 834x1112：侧栏收缩态不再泄漏到工作区。
- Mobile 390x844：不强求触屏拖拽，Tab 操作菜单提供 fallback，disabled `左移` 状态可见。

截图：

- `output/playwright/base-portal-v0.1.2-desktop-flyout.png`
- `output/playwright/base-portal-v0.1.2-tablet-834x1112.png`
- `output/playwright/base-portal-v0.1.2-mobile-390x844.png`

## 生产部署

已执行：

```bash
./upgrade.sh --from v0.1.1 --to v0.1.2 --image base-portal-release:v0.1.2 --pull never
```

启动窗口内出现过短暂 `curl: (56) Recv failure` / `curl: (52) Empty reply from server` 重试输出；脚本最终通过健康检查并退出 0。

## 远端读回

- `.deploy/version`：`v0.1.2`
- `deploy-db-1`：`postgres:16-alpine`，healthy。
- `deploy-web-1`：`base-portal-release:v0.1.2`，running，端口 `0.0.0.0:3000->3000/tcp`。
- image id：`sha256:b7706779de49c974169c355a36fcf0137f7d3db4bb0fa29f22134b03575d0ed1`
- 容器环境：`BASE_PORTAL_VERSION=v0.1.2`、`APP_VERSION=0.1.2`，`GIT_COMMIT` 由最终 release tag 和线上 `/version` 校验。

## 生产 HTTP 验证

- `https://base-portal.riversoft.com.cn/health`：`200 {"status":"ok"}`
- `https://base-portal.riversoft.com.cn/ready`：`200 {"status":"ready","checks":{"database":"ok"}}`
- `https://base-portal.riversoft.com.cn/version`：`version=0.1.2`
- `https://base-portal.riversoft.com.cn/`：`200 text/html`

## 生产 Playwright smoke

- 页面标题：`Base Portal`
- 关键元素：`Base Portal`、说明文案、`使用 Feishu IAM 登录`、`本地 mock 登录`
- Console errors：0
- Network：`/api/session` 返回 200
- 截图：`output/playwright/base-portal-v0.1.2-production-login.png`

## Secret 边界

- 未读取、未回显、未提交真实 `deploy/.env`。
- 未输出 IAM secret、developer API token、数据库密码、阿里云凭证或证书私钥。
