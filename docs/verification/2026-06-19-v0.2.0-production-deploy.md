# Base Portal v0.2.0 Production Deploy Verification

日期：2026-06-19

## 发布物

- Git commit：以 `v0.2.0` tag、`origin/main` 和生产 `/version.commit` 最终读回一致为准。
- GitHub Release：`v0.2.0`
- Docker image：`base-portal-release:v0.2.0`
- 远端 image：`sha256:5f1dd74d63a4f28a78bc50d2a59af38be4c4df47a348dd97201377e8c9e2a9a6`
- 架构：`linux/amd64`
- 部署目录：`bpmt@120.24.236.92:/home/bpmt/base-portal`

## 部署命令

首轮本机构建默认产出 `linux/arm64`，服务器为 `linux/amd64`，已修正为：

```bash
docker buildx build --platform linux/amd64 -f deploy/web.Dockerfile -t base-portal-release:v0.2.0 --load .
docker save base-portal-release:v0.2.0 | ssh bpmt@120.24.236.92 'docker load'
GIT_COMMIT=$(git rev-parse HEAD) ./upgrade.sh --from v0.2.0 --to v0.2.0 --image base-portal-release:v0.2.0 --pull never --force
```

## 生产读回

远端：

```text
deploy_version=v0.2.0
deploy-web-1 image=base-portal-release:v0.2.0
config_present=yes
```

远端本机健康检查：

```json
{"status":"ok"}
{"status":"ready","checks":{"database":"ok"}}
{"name":"base-portal-api","version":"0.2.0","commit":"<matches-v0.2.0-tag>","node_env":"production"}
```

公网健康检查：

```json
{"status":"ok"}
{"status":"ready","checks":{"database":"ok"}}
{"name":"base-portal-api","version":"0.2.0","commit":"<matches-v0.2.0-tag>","node_env":"production"}
```

DNS：

```text
dig @223.5.5.5 +short base-portal.riversoft.com.cn A -> 120.24.236.92
dig @8.8.8.8 +short base-portal.riversoft.com.cn A -> 120.24.236.92
```

证书：

```text
subject= /CN=riversoft.com.cn
issuer= /C=AT/O=ZeroSSL GmbH/CN=ZeroSSL ECC DV SSL CA 2
notBefore=May 29 00:00:00 2026 GMT
notAfter=Aug 27 23:59:59 2026 GMT
```

nginx：

```text
server_name base-portal.riversoft.com.cn
system-nginx Up
```

## 生产接入包与 IAM Developer API

生产容器内使用真实部署环境变量执行同一 `OpsService` 核心逻辑。输出仅包含 key、status 和计数。

`importAppPackage`：

```json
{
  "dryRun": true,
  "summary": {"domains":{"create":1,"update":0},"menus":{"create":3,"update":0,"disable":0},"permissionPoints":2,"permissionGroups":1,"bindings":2}
}
```

```json
{
  "dryRun": false,
  "summary": {"domains":{"create":1,"update":0},"menus":{"create":3,"update":0,"disable":0},"permissionPoints":2,"permissionGroups":1,"bindings":2}
}
```

`syncIamResources`：

```json
{
  "dryRun": true,
  "summary": {"permissionPoints":2,"permissionGroups":1,"bindings":2},
  "stages": []
}
```

```json
{
  "dryRun": false,
  "summary": {"permissionPoints":2,"permissionGroups":1,"bindings":2},
  "stages": [
    {"name":"list-permission-points","ok":true,"status":200},
    {"name":"list-permission-groups","ok":true,"status":200},
    {"name":"create-point:base-portal.demo.embedded","ok":true,"status":201,"key":"base-portal.demo.embedded"},
    {"name":"create-point:base-portal.demo.new-tab","ok":true,"status":201,"key":"base-portal.demo.new-tab"},
    {"name":"create-group:base-portal.demo.users","ok":true,"status":201,"key":"base-portal.demo.users"},
    {"name":"bind-group:base-portal.demo.users","ok":true,"status":200,"key":"base-portal.demo.users"}
  ]
}
```

审计读回：

```text
app_package_import: 1
iam_resource_sync: 2
```

## 生产权限过滤

```json
{
  "noPermissionDemoDomains": 0,
  "partial": [{"key":"demo-services","menus":[{"key":"demo-root","children":["demo-embedded"]}]}],
  "full": [{"key":"demo-services","menus":[{"key":"demo-root","children":["demo-embedded","demo-new-tab"]}]}]
}
```

## 真实 OAuth 用户路径

- `https://base-portal.riversoft.com.cn/api/auth/login` 已跳转到飞书登录页。
- 证据：`output/playwright/base-portal-v0.2.0-production-feishu-login.png`
- console error：`output/playwright/base-portal-v0.2.0-production-feishu-login-console-errors.json`，error 0。
- 当前 Codex 会话没有可用飞书用户登录态，未输入任何凭证，因此尚未完成真实用户 `code -> userinfo -> me/permissions` 的最终人机验收。

## Secret 边界

- 未读取或打印远端 `deploy/.env` 值。
- 未记录 client secret、developer API token、OAuth code、access token、cookie、数据库密码或证书私钥。
- IAM developer API apply 输出只保留 key、status 和计数。
