# Base Portal v0.1.1 生产部署验证

时间：2026-06-19 20:05 CST

## 目标

- 仓库：`wodenwang/base-portal`
- 版本：`v0.1.1`
- 服务器：`bpmt@120.24.236.92:/home/bpmt/base-portal`
- 域名：`https://base-portal.riversoft.com.cn`

## 本地门禁

- `pnpm check`：通过，API 2 tests、portal-web 13 tests。
- `pnpm build`：通过。
- `bash -n install.sh`：通过。
- `bash -n upgrade.sh`：通过。
- `BASE_PORTAL_IMAGE=base-portal-release:v0.1.1 docker compose --env-file deploy/.env.example -f deploy/docker-compose.yml config`：通过。
- `git diff --check`：通过。

## 远端部署

- 远端构建镜像：`base-portal-release:v0.1.1`
- image id：`sha256:2a34dff5b0ddbd987d4e41cb5922252fae74d89c3d54055dcb1bf90aacb98fd8`
- 升级命令：

```bash
./upgrade.sh --from v0.1.0 --to v0.1.1 --image base-portal-release:v0.1.1 --pull never
```

- GIT_COMMIT 修正后同版本重部署命令：

```bash
./upgrade.sh --from v0.1.1 --to v0.1.1 --image base-portal-release:v0.1.1 --pull never --force
```

启动窗口内出现过短暂 `curl: (56) Recv failure` / `curl: (52) Empty reply from server` 重试输出；脚本最终通过健康检查并退出 0。

## 远端读回

- `.deploy/version`：`v0.1.1`
- `deploy-db-1`：`postgres:16-alpine`，healthy。
- `deploy-web-1`：`base-portal-release:v0.1.1`，running，端口 `0.0.0.0:3000->3000/tcp`。
- 容器环境：`BASE_PORTAL_VERSION=v0.1.1`、`APP_VERSION=0.1.1`，`GIT_COMMIT` 由最终 release tag 和线上 `/version` 校验。

## 生产 HTTP 验证

- `https://base-portal.riversoft.com.cn/health`：`200 {"status":"ok"}`
- `https://base-portal.riversoft.com.cn/ready`：`200 {"status":"ready","checks":{"database":"ok"}}`
- `https://base-portal.riversoft.com.cn/version`：`version=0.1.1`
- `https://base-portal.riversoft.com.cn/`：`200 text/html`
- `https://base-portal.riversoft.com.cn/favicon.svg`：`200 image/svg+xml`

## Playwright 验证

- 页面标题：`Base Portal`
- 关键元素：`Base Portal`、说明文案、`使用 Feishu IAM 登录`、`本地 mock 登录`
- Console errors：0
- Network：`/`、JS、CSS、`/api/session`、`/favicon.svg` 均返回 200
- 截图：`output/playwright/base-portal-v0.1.1-production-login.png`

## Secret 边界

- 未读取、未回显、未提交真实 `deploy/.env`。
- 未输出 IAM secret、developer API token、数据库密码、阿里云凭证或证书私钥。
